# 项目协作与 GitHub 同步

## 用户的持续要求

- 完整实现保持 GitHub **私有**，不得擅自公开仓库、添加协作者或发布源码。
- 后续完成一个功能或修复，完成适用的验证后，提交本任务的代码和相关文档，并同步到既定 GitHub 远程仓库。用户已授权这一日常同步，无需每次重复询问。
- 只提交本任务应交付的改动，不将不相关的用户修改一并提交；不上传 `.env`、凭据、数据库备份、模型缓存、虚拟环境、原始录制帧。
- 按用户要求使用 Superpowers；具体框架/API 先查 Context7。前端直接迭代的例外遵循用户当前指示。开发过程及时追记相应 `dev-notes`，README 以安装、使用与功能为主。

## 同步方式

- 仓库内 `.githooks/post-commit` 与 `post-merge` 在当前仓库显式启用后，将已提交的当前分支推送到 `origin` 的同名分支。
- 自动推送只传提交，不自动暂存文件，不强推，不自动合并远程分歧。
- `origin` 的推送地址必须与本地 `support.expectedRemote` 完全一致；不得擅自更换远程地址。
- 正常完成任务后核对 `git status`、upstream 和远端提交。钩子输出失败时，本地提交仍保留；在授权范围内解决登录/网络问题并重试，未成功前不能宣称 GitHub 已同步。
- 功能分支推送不会更新默认分支。只有按用户选择合并完成后，默认分支内容才更新。
- 不需要后台定时任务或文件保存监听器。无法完成同步时，在交付说明中明确本地提交与远程的差异。
- 新克隆默认不启用本地钩子；设置方法和暂停开关见 `docs/github-sync.md`。
