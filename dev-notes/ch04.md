# 第04章：混合检索、重排与评估

## 2026-09-18｜需求接收与现有基础核对

- 用户关键原话：“Milvus原生BM25 + hybrid_search RRF融合”“dense向量和BM25各召回Top-50”“bge-reranker-v2-m3精排出Top-10”“检索不到或证据置信度低就显式拒答”“前端部分是例外，用Vibe Coding方式直接做”。
- 关键产出：按Superpowers brainstorming判定为新增检索子系统，走架构规格流程；现有基线为本地合并后的codex/ch01-chat（b33335d），开始时工作区干净。已核对当前query_faq只做MySQL字面LIKE，仓库没有第三章文档导入、chunk、embedding或Milvus代码。先补齐知识库来源和运行方案，再形成后端规格。前端按用户例外处理，不加入brainstorm/TDD/code review。
- 用户拒绝或纠偏：暂无。固定保留Milvus原生BM25/chinese analyzer、RRF、bge-reranker-v2-m3；未把缺失的第三章基础假设为已经存在。已询问使用演示知识库还是用户现有知识库/第三章成果。
- 翻车与返工：尚未实施，无代码返工。不能直接套用第二章“关键词必须原文、不扩展同义词”的限制，新章需要在检索侧明确调整；这会写入待审规格，尚未修改现有聊天行为。

## 当前流程清单

- [x] 核对项目现状与固定约束。
- [x] 确认知识库来源、dense模型和运行部署方案（真实运行兼容性待实施验证）。
- [x] 比较接入方案并定稿后端数据流、拒答和评估口径。
- [x] 编写并自审书面规格。
- [x] 用户审阅书面规格并通过。
- [x] 规格通过后使用writing-plans编写实施计划和评审。
- [ ] 后端逐任务TDD/评审，Prompt和数据逐项评估，前端Vibe Coding。
- [ ] 真实环境验收、最终代码评审与finish交付。

## 2026-09-18｜Context7与运行环境预检（规格待定）

- 用户关键原话：“一律先用Context7 MCP查最新官方文档和接口定义再动手”“版本对不上的API是返工重灾区”。
- 关键产出：Context7已解析并查阅 `/milvus-io/milvus-docs`、`/milvus-io/pymilvus`、`/flagopen/flagembedding`，核对BM25 Function、SPARSE_FLOAT_VECTOR、AnnSearchRequest过滤参数、hybrid_search与RRFRanker，以及bge-reranker-v2-m3的query/passage评分接口。本机arm64、24GiB内存/8核；Docker aarch64、8核、约7.75GiB内存；磁盘可用85GiB。当前venv没有pymilvus/torch/transformers/FlagEmbedding。
- 用户拒绝或纠偏：知识库来源问题仍待用户回答；尚未决定dense模型、安装依赖或拉取模型。指定重排模型未替换。
- 翻车与返工：Context7 Milvus文档默认返回v3.0.x内容，不能直接当作2.5/2.6已核验API。后续必须锁定服务端/SDK匹配版本并验证chinese analyzer对中英数字型号的实际分词；bge的normalize=True仅是sigmoid映射，不能直接当作已校准的置信概率。sandbox内sysctl受限，升级只读查询后获取资源数据，没有修改Docker配置。

已核对的官方来源：

- https://milvus.io/docs/full-text-search.md
- https://milvus.io/docs/chinese-analyzer.md
- https://github.com/milvus-io/pymilvus/blob/master/_autodocs/api-reference/rankers.md
- https://github.com/flagopen/flagembedding/blob/master/docs/source/bge/bge_reranker_v2.md
- https://huggingface.co/BAAI/bge-reranker-v2-m3

## 2026-09-18｜接收第三章 DDL，核查数据库实际状态

- 用户关键原话：“knowledge_chunks（知识库原文权威源）”“向量落 Milvus 集合 knowledge”“category + questions + answer 三格拼成向量化文本；其余字段是元数据，只存不进向量”，并提供 `knowledge_chunks`、`qa_extraction_staging` 两张表的完整 DDL。
- 关键产出：先通过 Context7 核对 SQLAlchemy 2.0 的异步 Inspector 用法（`AsyncConnection.run_sync`），再只读连接当前 `.env` 对应的 MySQL。实查仅存在 `conversations`、`faq`、`messages`、`tickets`；`knowledge_chunks`、`qa_extraction_staging`、`low_confidence_questions` 均不存在。本次未执行 DDL、未修改现有数据、未输出连接信息或密钥。
- 用户拒绝或纠偏：用户明确了知识库底座结构，后续规格以此为约束，不另起一套 chunk 原文模型。MySQL 保留 `id/category/questions/answer/section_path/content_type/is_key_clause/prev_chunk_id/next_chunk_id/vector_id/vectorize_status/created_at/updated_at`；Milvus 集合固定为 `knowledge`，主键与 chunk 对齐，`vector_id` 写入成功后回填。暂存表保留批次、来源、问答及 extracted/kept/discarded 状态；本次提供表结构尚不等同于要求实现整套历史会话抽取和去重流水线。
- 翻车与返工：没有执行失败或实现返工。提供了 DDL 不代表表和知识内容已经落地，已用实查排除该假设。`pending/done` 本身不能代替跨 MySQL/Milvus 的幂等写入协议；规格需要明确稳定主键 upsert、成功后标 done、失败后可重试。引用原文应回查 MySQL 权威 chunk，并展示章节路径。知识原文来源仍待确认，已询问先用带型号/政策/标注的电商演示知识库，还是使用用户现有资料/第三章成果。

本阶段新增核对来源：

- Context7 `/websites/sqlalchemy_en_20`：https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html
- Context7 `/websites/milvus_io`：https://milvus.io/docs/rag_pipeline.md 、https://milvus.io/docs/schema_design.md

## 2026-09-18｜确认演示知识库，提出接入方案

- 用户关键原话：“演示数据”。
- 关键产出：知识内容来源已确定为仓库内可追溯的电商演示资料。拟准备 120 个独立 chunk，覆盖商品型号/规格、退换货、运费、保修等主题，包含相近型号与不同品类的干扰条目；每条有章节路径，只按用户规定的 category/questions/answer 拼接入库文本。拟设 60 条正式评估问题及独立校准集，覆盖字面问法、具体型号、口语改写、同义词、品类过滤和无答案问题；标注相关 chunk、参考答案、类型与难度。数量和方案为当前设计建议，尚未生成数据或宣称评估完成。
- 用户拒绝或纠偏：知识来源问题已关闭；dense 模型及运行方式还未获确认。推荐扩展现有 `query_faq` 接入 RAG，保留同一聊天入口及订单/物流/工单工具；相比另设检索服务，该方案适合当前本机演示、部署组件较少。生成继续使用现有 DeepSeek；新增 dense 模型建议 BAAI/bge-m3，本机运行指定的 bge-reranker-v2-m3。该建议不替换用户指定的 Milvus 原生 BM25。
- 翻车与返工：无实现返工。本轮 Context7 确认 bge-m3 可以仅返回 dense_vecs（1024 维），关闭其 sparse 和 ColBERT 输出；中文关键词通道仍由 Milvus BM25 负责。Milvus Context7 再次返回 v3.0.x，已补查官方 v2.6.x chinese analyzer 页面；v2.6.x Docker 安装页经 web 工具访问失败，后续锁版本时需从官方发布材料核实，不能据此声称运行兼容性已通过。本轮未安装依赖、下载模型或修改数据库。

本轮设计参考：

- Context7 `/flagopen/flagembedding` 与官方模型卡：https://huggingface.co/BAAI/bge-m3
- https://huggingface.co/BAAI/bge-reranker-v2-m3
- https://milvus.io/docs/v2.6.x/chinese-analyzer.md

## 2026-09-18｜brainstorm 定稿与书面规格自查

- 用户关键原话：对“扩展现有 query_faq、本地 bge-m3、120 个 chunk、60 条正式评估、保留指定 BM25/重排”的方案询问“是否按这个方案定稿规格”，用户回复“是”；前一阶段已明确“演示数据”。
- 关键产出：写入 `docs/superpowers/specs/2026-09-18-ch04-hybrid-rag-design.md`，包含原始两表 DDL 附录、低置信度池、原文/索引指纹与幂等恢复、Top-50/RRF/Top-10、首尾证据布局、结构化自评、SSE 引用与拒答合同、120 chunk/60 正式问题/30 校准问题、指标口径及真实验收要求。方向已获用户批准；书面规格需单独审阅，尚未进入 writing-plans 或实现。自查覆盖范围、占位内容、一致性和歧义；明确前端只约定接口及用户验收，不纳入后台 brainstorm/TDD/code review。
- 用户拒绝或纠偏：本阶段无新增拒绝。落实用户 DDL 和演示数据选择，生成继续用现有 DeepSeek，新增本地 dense/reranker。第二章 keyword 字面子串限制在新章被显式替代；旧的四个业务工具保留 4 KB 结果限制，知识工具单独按证据预算控制。正式评估不得把无答案问题算 Recall 满分、不得把拒答算 Faithfulness 满分、不得拿测试集调阈值。
- 翻车与返工：规格自查发现“超限后重新自评”可能与每轮最多四个固定 LLM 阶段冲突，已改为先预留字段空间并裁剪、之后仅自评一次。新增 source_hash 排除 vectorize_status/vector_id/updated_at，避免标 done 本身导致指纹失配；补充过滤后仅 24 条候选时 Recall@50 的解释边界。Context7 再次给出 master/3.0 示例，改从官方 2.6.23 发布页核对服务端 2.6.23 / Python SDK 2.6.17 对应关系。Docker Hub 镜像清单只读检查发生网络超时，尚未确认 ARM64 镜像架构；未拉取镜像或启动新容器，不把网络超时当作技术栈不兼容。

本阶段仅修改文档，未运行新功能测试、未安装模型、未执行 DDL。已通过 7 项文档完整性检查（代码围栏、占位符、14 个设计节、DDL、指标合同、固定选型、行尾空白）及 git diff --cached --check；这些仅是文档检查，正式测试结果留待实际执行后记录。

## 2026-09-18｜书面规格通过，进入实施计划

- 用户关键原话：“规格通过”。
- 关键产出：将规格 `docs/superpowers/specs/2026-09-18-ch04-hybrid-rag-design.md` 状态更新为已批准；开始使用 writing-plans，围绕该规格定义共享接口、任务边界、RED/GREEN 与真实依赖验收。
- 用户拒绝或纠偏：无新增纠偏，不重新询问已经通过的数据来源、模型或架构选择；前端继续按 Vibe Coding 例外处理。
- 翻车与返工：只读查阅 Milvus v2.6.23 仓库中的 Compose 模板时发现其 Milvus 镜像仍标为 v2.6.22，计划会明确使用已选 v2.6.23 并实查服务版本，不能原样复制模板后声称符合版本要求。Docker Hub 网络和模型依赖可用性仍需实证核对，未执行建表、下载模型或业务实现。

## 2026-09-18｜实施计划评审通过

- 用户关键原话：沿用“规格通过”及“每完成一个阶段……就补一段”。
- 关键产出：`docs/superpowers/plans/2026-09-18-ch04-hybrid-rag.md` 拆为11个任务、46个步骤，明确跨任务DTO/接口、RED/GREEN命令、数据/Prompt评估、真实服务检查和前端Vibe例外。按writing-plans要求由控制器自审，覆盖映射无缺项；21段Python示例完成语法检查，代码围栏/占位符/空白检查通过。新跑基线 `pytest --require-mysql -q --tb=short`：244 passed、零跳过、1条既有弃用警告、24.67s。
- 用户拒绝或纠偏：没有新增纠偏；执行偏好已异步询问，推荐沿用上一章的当前目录功能分支+逐任务子代理。尚未把未回复视为创建额外worktree的同意。书面规格已通过，不重开架构选择。
- 翻车与返工：依赖dry-run在原锁约束下成功，选定FlagEmbedding1.4.2/pymilvus2.6.17/torch2.14.0/transformers4.57.6/sentence-transformers5.1.2/peft0.18.1；尚未实际安装和导入。已从PyMilvus v2.6.17源码核对hybrid_search的ranker参数与AnnSearchRequest过滤接口。计划自查修正了进度阶段缺少回调、共享模型工厂、取消后推理容量、跨worktree索引互斥等接口遗漏；将规格里容易误解的“人工抽查”澄清为标记复核者，助手复核不冒称用户人审。官方模型revision已记录，Docker Hub网络与镜像架构仍未验证。

本阶段产物为可执行计划与依赖预检证据，没有宣称新RAG功能已通过测试。

## 2026-09-18｜执行准备与 Task 1 开始

- 用户关键原话：沿用“规格通过”。执行偏好经过合理等待没有新增回复，采用已说明的推荐方式：当前目录功能分支、逐任务子代理和独立评审。
- 关键产出：计划提交 `28aa72a`；建立 `codex/ch04-hybrid-rag` 分支，未创建额外worktree。按subagent-driven-development建立本计划专属账本、11任务自检表和55个共享文件/接口关系检查行；Task1实现代理 `/root/ch04_storage` 从任务brief接收需求，控制器同时只读排查Docker Registry访问问题。
- 用户拒绝或纠偏：没有新增纠偏；没有将用户未回复当成创建worktree的同意。控制器负责统一过程日志、阶段复核与计划勾选，实现子代理只修改任务文件并报告RED/GREEN证据。
- 翻车与返工：技能辅助脚本缺少可执行权限，直接启动返回126；改为bash读取脚本并传入明确输出路径成功，没有修改插件安装文件权限。没有新增业务实现结果可供宣称完成。

## 2026-09-18｜Milvus 镜像网络预检完成

- 用户关键原话：沿用“规格通过”及固定 Milvus 选型要求。
- 关键产出：Docker Hub 官方标签接口确认 v2.6.23 有 linux/arm64；Docker Engine 成功拉取 `milvusdb/milvus:v2.6.23`，本地 inspect 确认为 linux/arm64，RepoDigest 为 `sha256:60e96d5abe7ea9199dd8a4d0d0506f9b57a7056cf57683327b8c7ae7f489ed17`。这只证明镜像下载与架构可用，真实服务/BM25 检查仍属于 Task4。
- 用户拒绝或纠偏：无新增纠偏；没有替换镜像来源或固定技术栈，没有更改系统代理、DNS 或 Docker 配置，也未停止当前 MySQL/聊天服务。
- 翻车与返工：buildx 清单查询曾直连超时，但 Python HTTPS 官方接口与 Docker Engine 拉取可用；采用已有 Engine 网络路径后成功。尚不能据此断言具体 DNS/代理根因，不进行无证据的全局网络修复。

## 2026-09-18｜配套镜像准备与发布地址修正

- 用户关键原话：沿用“规格通过”及“不要自行换方案”。
- 关键产出：etcd v3.5.25 与 MinIO RELEASE.2024-05-28T17-19-04Z 均成功拉取 linux/arm64 镜像；etcd digest `sha256:52f17f7e56e4f7239f0320dbfcbcc24721163d7d78ae710b466af3254ccf6366`，MinIO digest `sha256:391d1d45fdbe79944cb6de9337b073864bb9ee38c4c24280bfb39572e925af08`。计划准备段补充实际可用的官方 MinIO Quay 地址。
- 用户拒绝或纠偏：无新增纠偏；组件与固定版本不变，尚未启动新服务。
- 翻车与返工：Docker Hub `minio/minio` 拉取返回 access denied。核对固定版本官方 README 第26行明确使用 quay.io/minio/minio，再从该官方地址成功拉取；控制器把地址修正及若内容差异需重验的成本记录为 Ruling。没有使用不明第三方镜像或要求用户登录。

来源：https://raw.githubusercontent.com/minio/minio/RELEASE.2024-05-28T17-19-04Z/README.md；上游同症状记录：https://github.com/milvus-io/milvus/issues/53430 。

## 2026-09-18｜本地模型依赖安装预检

- 用户关键原话：沿用“规格通过”与“先用 Context7 MCP 查最新官方文档和接口定义再动手”。
- 关键产出：在 Task1 完成测试、独立评审期间安装计划 Task3 的已核对依赖，保持原 requirements.lock 约束；FlagEmbedding1.4.2、pymilvus2.6.17、torch2.14.0、transformers4.57.6、sentence-transformers5.1.2、peft0.18.1 安装退出0。BGEM3FlagModel/FlagReranker/MilvusClient/Function/FunctionType 导入退出0；pip check 显示 No broken requirements found。
- 用户拒绝或纠偏：无新增纠偏；没有变更技术选型，没有在 HTTP 请求中触发下载，尚未下载模型权重。仓库依赖清单与完整 lock 将由 Task3 实现一并更新。
- 翻车与返工：pip check 在沙箱内提示用户级 pip cache 不可写，但依赖一致性检查成功；安装使用既有允许的网络权限和 certifi CA，未禁用 TLS 或更改系统证书。安装/导入成功不代表真实推理、评分质量或性能通过。

## 2026-09-18｜Task1 首轮 code review：需要修正

- 用户关键原话：沿用“规格通过”和每个 code review 结论立即留痕要求。
- 关键产出：实现提交 `ea744be`；实现者真实 MySQL 全量回归255 passed、零跳过、1条既有弃用警告，DDL/仓储聚焦检查均通过。独立评审结论为规格未完全符合、质量需要修正：低置信度池的原话匹配依赖 MySQL 不区分大小写/重音的 collation，可能接纳不同原话。
- 用户拒绝或纠偏：无新增用户纠偏；评审确认原始知识两表DDL、文本拼接、源指纹和事务边界符合任务。控制器记录DDL断言完整性与既有警告两项minor，留给最终复核。
- 翻车与返工：进入第1轮修正，原实现者负责把原话比较改成精确相等并补大小写/重音差异的真实库回归；不会因原用例通过就忽略该缺口。

## 2026-09-18｜固定模型权重下载完成

- 用户关键原话：沿用已批准的本机 bge-m3 与 bge-reranker-v2-m3 方案。
- 关键产出：Context7 查询 Hugging Face snapshot_download，另核对已安装0.36.2实际签名后，两个固定 revision 权重下载退出0，分别位于 `.cache/ch04/models/bge-m3` 与 `.cache/ch04/models/bge-reranker-v2-m3`；约2.30GB/2.29GB，排除了ONNX等重复导出。
- 用户拒绝或纠偏：无新增纠偏；只下载官方仓库既定 revision，使用本地缓存，未修改应用接口。
- 翻车与返工：Context7 返回main版本HTTP客户端信息，与0.36.2有所不同，采用安装版实际签名和进程级certifi配置核对；权重下载成功，真实加载/推理尚待验证，不能据此宣称检索质量通过。

## 2026-09-18｜Task1 完成与修正复核通过

- 用户关键原话：沿用“规格通过”以及指定的 knowledge_chunks / qa_extraction_staging DDL。
- 关键产出：三张新表、原文与问题池仓储、不可变DTO、三格检索文本与源指纹完成；提交 `ea744be` 和 `2eee928`。修正复核结论为原话精确匹配问题已解决、无新增问题；Task1规格与质量门槛通过，计划四步已勾选。真实库DDL/事务/外键/幂等测试有证据；最终一次全量回归255 passed、零跳过、1条既有警告、23.63s。
- 用户拒绝或纠偏：无新增用户纠偏；控制器按阶段立即记录，不等待最终交付。DDL测试覆盖细度和原有警告的minor已记入账本，最终分支评审继续跟踪。
- 翻车与返工：大小写回归先报1 failed、6 passed，原话校验改为取回消息后进行Python精确比较，再跑7 passed；独立复核确认消除MySQL collation差异，没有新增技术选型或改动用户DDL。

## 2026-09-18｜离线 CPU 模型运行预检

- 用户关键原话：沿用已批准的本地模型方案。
- 关键产出：固定模型路径在离线模式、CPU、use_fp16=False下加载成功；两条短文本得到有限数值的2×1024 dense向量，相关/无关示例重排得分约0.999589/0.0000163，方向正确。进程总耗时4.91秒、峰值RSS约3.02GB，详细结果暂存本计划环境证据。
- 用户拒绝或纠偏：无新增纠偏；这只是环境小样本，不当作正式评估或50候选性能结论。
- 翻车与返工：没有运行异常；FlagEmbedding输出分词性能提示和进度条。Task3仍须实现并验证有界队列、取消、长度检查、正式下载脚本及真实语料批量计时。

## 2026-09-18｜Task2 实现与标注验证完成，进入独立评审

- 用户关键原话：“演示数据”“规格通过”。
- 关键产出：提交 `70d2c91` / `0eafd8e`，120条独立chunk、五类各24条、固定ID及来源清单；30条校准、60条正式标注，无跨集重复，含6条跨chunk问题。校验器13项聚焦测试通过，全量268 passed、零跳过、1条既有警告；真实MySQL两次导入分别0→120和120→120。资料评估记录见 `dev-notes/ch04-data-evaluation.md`，明确为助手复核。正在独立评审代码与标注。
- 用户拒绝或纠偏：无新增用户纠偏；同义词问法仅存在检索评估输入，未把同义词拆成重复入库条目。原DDL未增加source_key列，来源键留在资料清单。
- 翻车与返工：实现者先完成模块缺失RED，再完成校验接口缺失RED，最终GREEN；曾修正测试fixture章节路径预期和文件末尾空行。首次真实建表有MySQL整数显示宽度弃用提示，二次导入干净；应用没有清空数据或调用旧FAQ种子。

## 2026-09-18｜Task2 首轮 code/data review：需修正评估标注

- 用户关键原话：沿用“带难度梯度、标注 ground-truth 的评估集”和“规格通过”。
- 关键产出：独立评审确认语料数量、稳定ID、清单、严格加载器与专用导入入口，但规格与质量门槛未通过：test-literal-06引用刷头周期的ID错误；test-literal-02参考答案多加“只有”与完整包装清单冲突；test-model-09/10其实是通用政策，且前者混用了一个月与30天。
- 用户拒绝或纠偏：无新增用户纠偏；控制器逐一对照原文后确认三项重要问题，原实现者负责修正标注并重新冻结哈希。章节链闭环校验、演示电池保修表述及已有弃用警告记为minor供最终复核。
- 翻车与返工：数量/类型校验通过未发现语义标注错误，进入第1轮数据修正；本轮按用户要求用标注复核和评估校验替代纯数据TDD，不添加只检查答案字符串的伪单测。

## 2026-09-18｜Task2 完成与标注修正复核通过

- 用户关键原话：沿用“演示数据”“规格通过”。
- 关键产出：修正提交 `ff8f443` 通过独立复核，三项重要问题全部解决，无新增问题；Task2规格与质量门槛通过。最终120个chunk、30校准/60正式问题、75处证据引用、5个跨块问题，六个正式桶各10条，无跨集重复。源指纹 `843ba4d251f560b2ed61717f727c4fc46a34061d0a9f39f2ff098c1c3078e619`，正式集新哈希 `13a4a5af0896915e00ee11ed7446ff806a388a2da2b85bb3a0ecbd95a020cd00`，详见 `dev-notes/ch04-data-evaluation.md`。
- 用户拒绝或纠偏：无新增用户纠偏；纯标注修正用逐条来源对照和完整数据校验验收，代码测试证据沿用本任务最后一次13项聚焦/268项全量通过结果。
- 翻车与返工：刷头ID改为910058，包装答案去掉“只有”；两个伪型号问题替换为C65接口和R8-Max续航问题。复核确认新问题均有原文支持，月份边界歧义也已移除。
