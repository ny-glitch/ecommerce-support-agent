# ch07 · 当前会话上下文管理

## 2026-09-22 · 需求接收与现状核对（brainstorm 进行中）
- 用户关键原话：“最近的留原文，一个字都不压”“降级只挪这两个 id、不搬数据”“全程不阻塞用户当前这轮回复”“默认窗口下聊二十轮，不该触发任何降级和摘要”。范围仅当前会话，不跨会话、不存用户画像；增加会话侧栏和两个只读接口。
- 关键产出：按架构级 Superpowers 流程开始。核对基线 `6d8add1`：现有 `WorkflowState.history` 没有 `add_messages` reducer，prepare 和 persist 都裁历史；MySQL 当前实际保存完整工具结果；默认/本地窗口 8192、最多保留 12 轮，token 估算采用 UTF-8 字节数。已核对安装版本 LangGraph 1.2.11、langchain-core 1.6.3、SQLAlchemy 2.0.54、FastAPI 0.141.1。尚未修改产品代码、数据库或运行配置。
- 拒绝或纠偏：技术栈沿用 LangGraph + LangChain + MySQL，现有 PostgreSQL checkpointer 保留；新需求要求工具结果不落 messages 表，将在设计中区分 MySQL 可回载对话与 checkpoint 完整工具历史。旧会话和既有记录不删除。已提出唯一待确认项：示例预算 5650 / 3954 / 1695 是否必须精确匹配，或按真实开销与统一 token 口径推导后列出明细。
- 翻车与返工：Context7 的 LangGraph 文档检索偏到 DeltaChannel；不因此更换用户指定的 add_messages，改查官方 Graph API 与已安装版本。LangChain trim_messages 的 max_tokens、token_counter、allow_partial、start_on 等接口已通过 Context7 核对。后续设计仍需处理完整工具配对、异步摘要与活跃会话的竞争、旧 checkpoint 兼容、摘要提示词样例评估及两组 20+ 轮演示。

### 流程检查项
- [x] 澄清预算验收口径，展示方案与设计
- [x] brainstorm 定稿，写入并自审规格
- [ ] 用户确认书面规格
- [ ] 计划评审通过，确认执行方式
- [ ] 按任务实施；可测试代码 TDD，Prompt 使用标注样例评估
- [ ] code review、完整验证与两套演示
- [ ] finish、功能演示命令、结果记录及私有 GitHub 同步

## 2026-09-22 · 预算口径澄清
- 用户关键原话：“按实际开销计算，日志列清扣除项（推荐）”。
- 关键产出：将 5650 / 3954 / 1695 视作原示例，实际验收采用本项目真实固定前缀、工具 Schema、检索证据、摘要与 ReAct 峰值推导出的预算；日志要逐项列出，不能硬编码结果。示例环境变量及默认/演示两种压缩行为验收继续保留。
- 拒绝或纠偏：不要求凑数；中文 token 估算和全部扣除项一起校准，不能只缩小模型窗口。现有累计调用预算 `turn_model_budget` 与单次模型窗口是不同约束，设计需分别保留，避免升级窗口后被旧的累计硬上限截断。
- 翻车与返工：尚无实现返工。现状进一步确认：已有故障恢复严格比较 MySQL 工具流水与 checkpoint，停止存工具结果时必须连同恢复协议升级；不能仅去掉一次 append_result 调用。

## 2026-09-22 · 方案提出（待确认，尚未定稿）
- 用户关键原话：“按实际开销计算，日志列清扣除项（推荐）”；原需求要求后台摘要“不阻塞用户当前这轮回复”。
- 关键产出：拟采用应用生命周期管理的后台摘要任务，任务状态/摘要落 MySQL，失败可重试、重启可恢复；相比独立 worker，当前单进程运行少一个服务。每个任务冻结覆盖区间，完成后只原子提交本次摘要及已覆盖边界，不在后台并发修改活跃 graph 的 checkpoint；下一轮装载摘要快照。完整历史与调用视图分开；两个锚点按完整轮次推进，保持 tool_call 与 tool_result 配对。
- 拒绝或纠偏：固定前缀不掺摘要/检索证据；旧摘要只读、不可再次合并；演示期沿用现有 demo 用户，不顺带新增登录系统。默认应用窗口拟提升为 128000、目标保留 32 轮，仍由环境配置覆盖；演示使用用户指定的 18000 等参数，实际预算另行校准。该默认值尚属设计提议，不是已改动配置。
- 翻车与返工：官方 DeepSeek 文档直读超时，官方搜索结果确认当前 deepseek-flash 支持 1M 上下文，因此 128000 作为应用侧窗口在其模型能力内；未调用付费模型或改动 `.env`。待用户确认设计方向后才形成正式规格。

参考：[DeepSeek 模型说明](https://api-docs.deepseek.com/quick_start/pricing/)。

### 已查文档
- Context7：`/websites/langchain_oss_python_langgraph`（状态、持久化）；文档结果需与固定安装版本交叉核验，不采用无关的 DeltaChannel。
- Context7：`/websites/reference_langchain` → [trim_messages API](https://reference.langchain.com/python/langchain-core/messages/utils/trim_messages)。
- 补充官方文档：[Graph API — messages 与 reducers](https://docs.langchain.com/oss/python/langgraph/graph-api)。

## 2026-09-22 · brainstorm 定稿与书面规格自审
- 用户关键原话：“确认”，批准应用内后台摘要 + MySQL 持久化任务状态的设计方向；此前已选择“按实际开销计算，日志列清扣除项”。
- 关键产出：形成 `docs/superpowers/specs/2026-09-22-ch07-session-context-design.md`，共 18 节，覆盖数据职责、两个锚点、分段摘要/任务表、非阻塞调度、State reducer、固定前缀、统一预算、v1→v2 恢复、两个只读接口、前端和两组 24+ 轮验收。通过 Context7 补查 SQLAlchemy 异步会话隔离、FastAPI lifespan，以及 add_messages 同 ID 替换语义。规格自审完成：无占位项，需求/验收有对应章节，文档结构、代码围栏和 whitespace 检查通过。
- 拒绝或纠偏：设计批准不等于批准尚未存在的实施计划；此阶段只写规格，产品代码、`.env`、数据库和运行服务未改动。后台摘要不修改活跃 checkpoint；新协议不把完整工具结果藏进 MySQL event_data；只读接口不触发恢复写入；不通过修改目标预算去凑原示例数字。
- 翻车与返工：自审明确了冻结目标而非完成时的新层 1 起点、纯闲聊 no_facts 的边界推进、摘要积压时的硬窗口兜底、缺失 v2 checkpoint 不能凭 MySQL 伪造工具历史，以及子图/父图 reducer 的去重约束。阶段验证仅针对规格，不将文档检查称为功能测试；等待用户审阅书面规格后再进入计划。
