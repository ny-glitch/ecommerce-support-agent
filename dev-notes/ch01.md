# 第 01 章开发记录：纯对话客服

本文件按阶段即时追加。尚未完成的阶段不记为通过。

## 2026-09-16｜启动与技术核对（brainstorm 进行中）

- 用户关键原话：
  - “我要用 Superpowers 模式做一个电商智能客服系统，第一步先跑通纯对话。”
  - “Python + FastAPI + LangChain”；“模型接入直连上游，应用侧统一说 OpenAI 协议”。
  - “工具调用、Agent 循环”本章不做。
  - “一律先用 Context7 MCP 查最新官方文档和接口定义再动手”。
  - “实现中发现矛盾或走不通，停下来问我，不要自行换方案”。
  - 每个阶段即时记录；纯 Prompt / 数据改用评估集验证；聊天页面采用 Vibe Coding，不套 brainstorm、TDD、code review。
- 关键产出：
  - 仓库检查：空 Git 仓库，master 尚无提交，没有既有代码或 AGENTS.md。
  - 已读取 Superpowers using-superpowers、Codex 工具适配说明及 brainstorming 技能；按新项目的 Architectural 路径推进。
  - 已通过 Context7 检索 FastAPI、LangChain 官方资料。FastAPI 当前文档提供 `fastapi.sse.EventSourceResponse` 和 `ServerSentEvent`；实现前还需核对实际安装版本及接口。
  - Context7 的 LangChain 查询确认 `with_structured_output` 的 json_mode / json_schema / function_calling 区别，但未准确返回 ChatOpenAI 的完整签名；实现前必须进一步核对，不能沿用 ChatAnthropic 签名。
  - 官方资料核对：Claude 的 OpenAI 兼容接口支持流式对话，但忽略 `response_format`。统一协议下的结构化提取策略需要用户确认。
  - 已形成待评审设计草案：`docs/superpowers/specs/2026-09-16-ch01-chat-design.md`。当前不是定稿，未开始应用实现、依赖安装或测试。
- 用户拒绝或纠偏：截至本条记录没有新的反馈；原始约束已纳入设计，不自行引入代理网关、原生供应商 SDK、工具调用或 Agent。
- 翻车与返工：文档检索一度返回 ChatAnthropic 的结构化 API，而非目标 ChatOpenAI；已识别并排除该签名。无实现返工。

### 已查阅资料

- Context7：`/websites/fastapi_tiangolo`，SSE 当前接口。
- Context7：`/websites/reference_langchain`，结构化输出方法与 ChatOpenAI 检索。
- FastAPI SSE：https://fastapi.tiangolo.com/tutorial/server-sent-events/
- LangChain ChatOpenAI：https://docs.langchain.com/oss/python/integrations/chat/openai
- Claude OpenAI 兼容性：https://platform.claude.com/docs/en/cli-sdks-libraries/libraries/openai-sdk

## 2026-09-16｜brainstorm 定稿

- 用户关键原话：“同意”。确认对象为上一轮设计草案、Claude JSON 生成依赖提示词并由应用校验的兼容策略，以及按上游增量立即推送的 SSE 验收口径。
- 关键产出：`docs/superpowers/specs/2026-09-16-ch01-chat-design.md` 已改为定稿。自审覆盖范围、接口、失败语义、上下文、预算、Prompt 评估与真实验收；采用内存会话、单 worker、统一 OpenAI 协议及 json_mode。下一阶段使用 writing-plans 编制实施计划。
- 用户拒绝或纠偏：无；用户明确接受上一轮方案，没有放宽原先固定技术栈及禁止工具调用、Agent 的约束。
- 翻车与返工：无新增；前期检索未命中的 ChatOpenAI 完整签名仍须在实施前补查。

## 2026-09-16｜计划评审通过

- 用户关键原话：延续“同意”和“全程走 Superpowers 流程”；“产出不是可单测代码的任务……把 TDD 那步换成拿标注样例或评估集跑一遍验证”。
- 关键产出：`docs/superpowers/plans/2026-09-16-ch01-chat.md`，包含四个任务及接口、RED/GREEN、传输验证和真实评估步骤。writing-plans 自审通过：覆盖全部 spec、接口命名一致、未把替身测试冒充真实验收。采用推荐的子代理实现+任务评审；工作目录偏好异步征询期间不创建新 worktree，先在当前目录创建功能分支。`.gitignore` 作为执行前准备先建立，以免追踪密钥/临时报告。
- 用户拒绝或纠偏：无新增回复；没有把未回复视为另建 worktree 的同意。
- 翻车与返工：沙箱内网络无法解析 PyPI；允许网络后定位到本机 Python 默认 CA 文件缺失。使用现有 certifi CA 且保留 TLS 校验，成功核对 FastAPI 0.141.1、langchain-core 1.6.3、langchain-openai 1.6.2 等发布版本；没有关闭证书验证或修改系统配置。Context7 部分结果混入其他供应商/JavaScript 示例，实施须以 Python 安装包签名作最终核验。

## 2026-09-16｜Task 1 完成：配置与上下文预算

- 用户关键原话：沿用“历史消息裁剪加 token 预算控制”“地址、模型名、密钥全在 .env”；本阶段没有新的用户反馈。
- 关键产出：提交 `6177bde`，新增配置、`.env.example`、完整轮次上下文裁剪及安全异常。行为 RED 为 11 failed / 5 passed，最终 `.venv/bin/python -m pytest -q` 为 17 passed；`pip check`、编译及 diff 检查通过。独立任务评审结论：Spec compliant，Task quality Approved，无 Critical/Important/Minor；主控确认本地 `.venv` 的实际包导入/签名和忽略规则。计数按 UTF-8 字节加消息开销做保守估算，不宣称精确 token 数。
- 用户拒绝或纠偏：无。实现细化：增加 `LLM_TOKEN_LIMIT_PARAM`（默认 `max_completion_tokens`，可选 `max_tokens`）。Context7 官方 DeepSeek/Ollama 文档列出旧字段，而新版 ChatOpenAI 会改名；下一任务通过 extra_body 和发包测试保证选择生效，仍维持统一 OpenAI 协议。配置错误的代价是需调整一个 `.env` 字段，不静默切换供应商。
- 翻车与返工：自审发现标准库 URL 解析允许非法空白主机，补充回归用例观察 1 failed / 16 passed 后，改为已查文档的 Pydantic HttpUrl 校验，最终 17 passed。Superpowers 辅助脚本在插件缓存中缺执行位，使用 bash + 显式输出路径调用，没有修改插件。

## 2026-09-16｜Task 2 完成：模型适配、PromptTemplate 与结构化提取

- 用户关键原话：沿用“用 PromptTemplate 模板化”“用 with_structured_output 实现”“工具调用、Agent 循环，先跑通纯对话”。本阶段没有新增用户反馈。
- 关键产出：提交 `550361e`。真实 ChatOpenAI + HTTPX 网络边界测试覆盖 JSON mode、无 tools、两种输出上限字段、流增量、结束原因、取消关闭和严格售后字段校验。行为 RED：25 failed / 6 passed；最终模型测试 33 passed，全量 50 passed。Wheel 包已验证包含并能读取两份 Prompt。独立评审 Spec compliant / Task quality Approved，无问题；评审针对性检查确认关闭流迭代器会关闭底层 HTTP 流，并非测试靠关闭整个客户端掩盖泄漏。Prompt 语义评估和真实模型效果尚未验证。
- 用户拒绝或纠偏：无。异步询问真实上游后，仍未发现项目 `.env`、对应环境变量或本机 Ollama 服务；继续离线实现，不擅自下载模型或启用假模型作真实验收。
- 翻车与返工：LangChain 可能修补 partial JSON，因此增加对 raw 内容的严格 JSON 与 schema 校验。首轮 28 passed / 3 failed：SDK 在长度截断时直接抛异常，补充已查文档的异常映射；底层流关闭在下一事件循环 tick 完成，测试改为条件等待；修正空字符串“未泄漏”断言。新增重试关闭验证，移除 max_retries=0 时观察 1 failed，恢复后通过。未更换协议或引入工具调用。

## 2026-09-17｜Task 3 实现完成：会话与 SSE，独立评审进行中

- 用户关键原话：沿用“支持多轮对话，SSE 流式输出”“连续问两轮，第二轮能接住第一轮的上下文”；本阶段没有新增用户纠偏。
- 关键产出：实现提交 `6a8f66b`，路径修正 `48b6847`。API/会话首轮 19 项 RED；真实网络首段测试先通过，断连/超时 4 项 RED 后修复；最终 28 项聚焦通过、全量 78 passed。涵盖容量、TTL、会话并发冲突、完整轮次提交、中文 SSE、超时与闲置断连。关闭闲置断连检查的 mutation 会使对应真实连接测试失败。独立任务评审已开始，尚未记为评审通过。
- 用户拒绝或纠偏：无。主控依据定稿 spec 纠正提取路径为 `/api/after-sales/extract`，没有添加多余别名或更换协议。
- 翻车与返工：本机 HTTPX 自动读取 macOS 系统代理，回环测试实际连到 127.0.0.1:7890，造成断连清理的假失败；仅测试客户端改为 `trust_env=False` 后验证通过，生产代理行为未改。实现误用了 `/api/extract`，改测试先观察 5 项 404 RED，修正后提取聚焦 6 passed。测试保留 1 条 Starlette 1.6.0 使用 AnyIO 4.15.1 弃用别名的依赖警告，未更换依赖或掩盖。Git 首次自动审批超时，重试成功，没有遗留审批阻塞。

### Task 3 独立评审结论

- 用户关键原话：延续“全程走 Superpowers 流程”；无新增反馈。
- 关键产出：评审范围 `61efa0f..48b6847`，Spec compliant / Task quality Approved；无 Critical 或 Important。评审检查了路由取消与模型迭代器关闭的跨模块关系，确认清理传递。Task 3 阶段完成。
- 用户拒绝或纠偏：无；真实模型与 Prompt 语义仍待本地配置，未宣称通过。
- 翻车与返工：无新增代码问题；1 条上游依赖弃用 warning 记为非阻塞 Minor，等待兼容的上游修正，当前不因它改变已验证版本。

## 2026-09-17｜Task 4 实现完成：标注评估与交付脚本，独立评审进行中

- 用户关键原话：沿用“拿标注样例或评估集跑一遍验证”“完结交付：功能演示命令、测试结果、dev-notes 路径”；本阶段无新增反馈。
- 关键产出：提交 `ca288da`，包含 `evals/cases.json`（9 个提取标注、5 个客服人工评阅样例）、真实 HTTP 评估器、两轮 curl 演示、README 和精确依赖锁。评估器测试先失败，后 7 passed；演示失败分支补齐后 11 项聚焦通过，最终全量 89 passed / 1 条已知依赖 warning。pip check、语法、JSON、锁文件与当前环境一致性、diff 检查通过。独立任务评审进行中。
- 用户拒绝或纠偏：无。仍无真实模型配置，因此 Prompt 标注评估尚未执行，报告将自动提取结果、请求失败、人工语义待评阅分开，不能把替身当成模型质量证明。
- 翻车与返工：测试的本地 HTTP 绑定最初被沙箱拒绝，提权到仅本机测试后通过；未修改生产逻辑绕过。演示脚本尚未存在时成功用例失败，实现后覆盖 HTTP 失败、SSE error、缺 done 的非零退出。主控另外运行真实 curl → Uvicorn/FastAPI → ChatOpenAI/with_structured_output → HTTPX 替身的完整协议链路，且实际 `demo.sh` 退出 0、两轮复用 UUID、返回三字段 JSON；输出明确标注“离线协议替身”，不作为语义评估。临时服务已关闭。

### Task 4 评审退回：会话元数据误判成功

- 用户关键原话：沿用“验收标准”“过程留痕”；无新增用户反馈。
- 关键产出：独立评审为 Spec issues found / Needs fixes，发现 `scripts/evaluate.py` 接受任意字符串的 meta session_id，且仅判断 done 存在。主控核对确认后交原实现者修复，要求 UUID 有效、done 为对象并匹配 session_id，后续轮也必须复用请求会话。
- 用户拒绝或纠偏：无；这是依据既定协议的代码评审纠偏，不涉及技术栈更换。
- 翻车与返工：主控此时重跑全套仍为 89 passed / 1 warning，说明既有测试遗漏了这条假成功路径；正在补有区分力的回归用例。此条即时记录，尚未宣称修复完成。

### Task 4 修复复审通过，阶段完成

- 用户关键原话：延续既定验收和留痕要求；无新增用户反馈。
- 关键产出：修复提交 `f1b2d2e`，3 项回归用例 RED（错误流原先 exit 0），修复后评估聚焦 14 passed。限定范围复审确认 finding ADDRESSED，无新增问题，Task 4 完成。主控在修复后重跑全套：92 passed / 1 条已知依赖 warning；pip check、shell 语法、diff 和锁文件一致性检查通过。
- 用户拒绝或纠偏：无。依旧不把未配置的真实模型标为验收通过。
- 翻车与返工：会话元数据误报路径已修正；没有改动 API、Prompt、模型适配器或演示脚本。此前实际 demo.sh 的离线完整链路结果继续有效。下一阶段是整个分支的最终 code review。

### 过程核对：标准库 HTTP 文档补查

- 用户关键原话：“涉及具体库、框架、API 的用法……一律先用 Context7 MCP 查最新官方文档和接口定义再动手”。
- 关键产出：补查 Context7 `/python/cpython/v3.13.9` 官方 urllib.request、http.server/socketserver 文档，并在当前 Python 3.13.2 核对实际签名。Request、ProxyHandler、opener.open、ThreadingHTTPServer 及关闭流程用法一致，无版本冲突，无需代码更改。
- 用户拒绝或纠偏：无用户新反馈；主控自查发现并纠正流程遗漏。
- 翻车与返工：Task4 在实现评估器时只复用既有 FastAPI/SSE 资料，标准库 HTTP API 未事先单独查询。这是事后补查，不能声称全程每个 API 都做到先查后写；已如实留痕。此前的真实 HTTP 测试和92项通过结果仍有效。

## 2026-09-17｜最终 code review：打包安装需修复

- 用户关键原话：沿用固定技术栈与“完结交付：功能演示命令、测试结果、dev-notes 路径”；无新增反馈。
- 关键产出：整个分支 `8caeccc..e0b330a` 总评审，Critical 0，Important 1：新增 `evals/` 后 setuptools 默认自动发现多个顶层包，安装命令会失败。其余应用逻辑与任务衔接未发现阻塞问题。已在修复前通过 Context7 核对 setuptools 的显式包发现配置。
- 用户拒绝或纠偏：无。独立评审指出 Task2 的 wheel 验证早于 Task4 的目录增加，不能证明最终目录的可安装性。
- 翻车与返工：主控用已提交内容的临时副本执行真实 wheel 构建，确认多顶层包发现错误；首次把 wheels 输出目录放在副本内部，还额外引入一个被发现目录，随后把输出移出源码副本做精确复现。将进行一次最终修复及限定范围复审。既有 Starlette/AnyIO 弃用警告仍为非阻塞 Minor，真实模型与 Prompt 语义仍待配置。

## 2026-09-17｜最终 code review 通过与离线验证结论

- 用户关键原话：沿用“全程走 Superpowers 流程”“过程留痕”“功能演示命令、测试结果、dev-notes 路径”；本阶段没有新增用户反馈。
- 关键产出：修复提交 `39a5434`，仅在 `pyproject.toml` 显式发现 app/app.* 并关闭 namespace 发现。全新临时 venv 安装锁依赖、editable 安装成功；源目录外构建并安装 wheel，包含两份 Prompt、不包含 evals，仓库外成功渲染模板。最终限定复审结论：Important finding ADDRESSED，无新增 Critical/Important；已知依赖 warning 确认为非阻塞。主控在此提交上全量运行 **92 passed, 1 warning in 10.73s**；pip check、锁文件一致性、shell 语法、diff 检查全部通过。
- 用户拒绝或纠偏：无。对残留 Minor 的处置：保留已验证的 Starlette 1.6.0 / AnyIO 4.15.1，记录上游弃用别名，待兼容的上游版本修复后处理，不为提示随意变更依赖。
- 翻车与返工：最终目录出现的多顶层包安装问题已通过真实构建从 RED 到 GREEN 验证；没有复用旧目录的打包结论。客服 API、模型协议和演示逻辑未因打包修复改变。

## 2026-09-17｜交付状态与 finish 待办

- 用户关键原话：“curl 调对话接口能看到流式回复”“连续问两轮，第二轮能接住第一轮的上下文”“发一段售后描述，能拿到结构化 JSON”。
- 关键产出：代码、README 的启动及三条 curl、`scripts/demo.sh`、`scripts/evaluate.py`、9 个提取标注和5个客服人工 rubric 均已就绪。完整离线协议演示成功，代码与安装评审完成。代码保留在 `codex/ch01-chat`；没有配置远程仓库，没有合并或推送。
- 用户拒绝或纠偏：无新增拒绝；已请求仅在本地 `.env` 配置 `LLM_BASE_URL`、`LLM_MODEL`、`LLM_API_KEY`，不在聊天中发送密钥。截至最后核对，这三项环境变量和 `.env` 均不存在，也无可用本地 Ollama 服务。
- 翻车与返工：真实模型三项验收与 Prompt 标注语义评估尚未执行，不能用替身演示补足它们。**finish 尚未完成**：待真实上游配置后运行 demo/evaluate，人工审阅客服 rubric、记录模型结果，再完成分支收尾。缺少真实凭证是当前剩余阻塞，未伪造评估分数。

## 2026-09-17｜真实上游选择：DeepSeek，等待本地密钥

- 用户关键原话：“这一段具体需要我怎么做？”随后选择“deepseek”。
- 关键产出：先通过 Context7 `/websites/api-docs_deepseek` 及官方 API 文档核对当前型号与参数，再将被 Git 忽略的本地 `.env` 设置为 `LLM_BASE_URL=https://api.deepseek.com`、`LLM_MODEL=deepseek-flash`、`LLM_TOKEN_LIMIT_PARAM=max_tokens`。保留原密钥行，未打印密钥；配置解析通过，当前密钥仍为占位值。用户只需在本地填写 API Key。
- 用户拒绝或纠偏：用户明确选择 DeepSeek；没有更换 Python/FastAPI/LangChain 或 OpenAI Chat Completions 协议。
- 翻车与返工：检索的旧页面仍出现 deepseek-chat/旧型号别名，官方当前首页与接口定义列出 deepseek-flash，故不按旧记忆填值。官方当前模型默认启用思考，纯聊天无需回传 reasoning_content；现有应用只发送完整对话并转发回答内容。实际延迟、输出上限和 JSON 行为需在收到有效密钥后验收，尚未调用模型或宣称通过。没有改动应用代码或预算参数。
- 资料：https://api-docs.deepseek.com/ ，https://api-docs.deepseek.com/api/create-chat-completion/ ，https://api-docs.deepseek.com/guides/thinking_mode/

## 2026-09-17｜真实验收首次执行：DeepSeek 余额不足

- 用户关键原话：“填好了”。授权继续使用已选择的 DeepSeek 和本地配置执行真实验收。
- 关键产出：配置解析成功、密钥已非占位值，未输出密钥。使用 `BASE_URL=http://127.0.0.1:8001 bash scripts/demo.sh`，首轮收到 SSE meta 后返回 UPSTREAM_ERROR，退出1，未完成任何真实对话轮次。脱敏诊断仅发送固定连通性测试文本，确认上游 HTTP **402 Insufficient Balance**；Context7 与 DeepSeek 官方错误码文档核对为 API 账户余额不足。报告保存在被忽略的 `evals/reports/2026-09-17-deepseek-preflight.json`。
- 用户拒绝或纠偏：无用户拒绝。自动审批首次因示例姓名和订单可能涉及敏感数据外发而拦截；核对脚本全文、提交 ca288da 和计划后，证实全部是助手生成的固定虚构测试数据，不来自实际客户资料。携带这些证据重新申请后获准执行，没有绕过审批。
- 翻车与返工：8000 已被另一个项目占用；核对监听者后未停止该进程，另启本项目8001实例。真实调用受余额限制，停止后续标注评估以避免重复失败。8001 验收服务已正常退出，8000 原进程保持不变。未改应用代码、密钥、预算或模型，未记录真实模型通过。
- 当前待办：用户在 DeepSeek 开放平台补充 API 账户余额后重跑验收；配置缺失已解决，当前阻塞是上游余额不足。资料：https://api-docs.deepseek.com/quick_start/error_codes/

## 2026-09-17｜真实三项验收通过：DeepSeek 充值后重跑

- 用户关键原话：“已充值”。继续执行已约定的真实模型验收与标注评估。
- 关键产出：本机8001实例使用 `.env` 的 `deepseek-flash` / max_tokens=1024；`BASE_URL=http://127.0.0.1:8001 bash scripts/demo.sh` 退出0。首轮收到多次 token 后 done；第二轮复用 `32ad108b-d678-4a95-a679-e5c292b91577`，实际回答“您叫小林；您反馈的问题是刚买的耳机有杂音。”；售后返回 `{"order_id":"A123","request_type":"exchange","expected_resolution":"换一个新的"}`。完整无密钥输出保存于 `evals/reports/2026-09-17-deepseek-demo-after-topup.log`。
- 用户拒绝或纠偏：无。继续沿用已批准的虚构验收文本与固定技术栈；没有输出或提交密钥。
- 翻车与返工：此前的402余额不足已不再出现，本轮未改代码或配置预算。原有8000其他项目保持不变。三项演示通过，9个提取标注与5个客服 rubric 评估尚在下一阶段，不把演示当成评估全通过。

## 2026-09-17｜真实 Prompt 评估基线与返工计划

- 用户关键原话：延续“已充值”和“纯 Prompt、数据类……拿标注样例或评估集跑一遍验证”。
- 关键产出：原始报告 `evals/reports/2026-09-17-deepseek-evaluation-initial.json`：9个提取严格匹配6通过/3失败，5个客服已取得完整真实输出，HTTP错误0。人工核对：missing_order 的“换一个完好的玻璃杯”与标注语义等同，但超出既有等价列表；其余2个歧义售后返回unknown而标签为other，是真实分类边界问题。客服在未提供政策时举具体退货类别，并对简单退货一次追问6项，需收紧约束。
- 用户拒绝或纠偏：无新增用户纠偏；主控按已有简明客服和不编造政策要求评估，不修改原始标签来凑分。既有演示通过不等于Prompt评估全通过。
- 翻车与返工：初版“没有明确诉求”措辞把类型未选定也归入unknown；初版客服约束仅禁止期限/补偿，未明确禁止推测政策类别。已在 `docs/superpowers/plans/2026-09-16-ch01-chat.md` 增补 Task5 并自审通过；两份Prompt做最小修正，另加事先标注的补充样例。没有框架/API或技术选型变化。

## 2026-09-17｜Task 5 Prompt 实现完成，真实复测进行中

- 用户关键原话：沿用“纯 Prompt、数据类……拿标注样例或评估集跑一遍验证”；无新增用户反馈。
- 关键产出：独立实现者提交 `8dbc06f`，仅改两份 Prompt，渲染检查通过。主控在复测前另编 `evals/ch01-regression-cases.json`（6提取、4客服），SHA256为 `118a586c58acd9afcb3814ef9dddb33dfded071b7f08043de3f93957bdcb9214`；实现者未读取补充集。原始与补充真实评估均已启动，尚未给出通过结论。
- 用户拒绝或纠偏：无；原始标签不变，修正没有改变接口、字段或供应商。
- 翻车与返工：全套离线回归出现90通过/2失败/1依赖警告；两项测试未隔离真实 `.env`，DeepSeek的max_tokens污染了“默认max_completion_tokens”的断言。属于测试环境问题，生产读取配置行为符合预期。已追加Task6并自审，范围仅测试隔离，保留真实dotenv集成验证，用户 `.env` 不改动。Context7核对 `_env_file=None` 禁用dotenv但不禁用进程环境变量；计划同步首次patch上下文不匹配，重新精确更新后成功，未影响应用。
